وضع داخله ملفات ترجمة (مثلاً: `en.json`, `ar.json`) عند تفعيل التعدد اللغوي.

---

## 🛠 لا تنس تحديث `pubspec.yaml`:
تأكد أن ملف `pubspec.yaml` يحتوي على:

```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/translations/